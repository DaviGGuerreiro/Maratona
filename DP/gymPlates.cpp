#include<bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);cin.tie(0);
    int t, n;
    cin>>t;
    vector<vector<int>> dp ((1<<10), vector<int> (n,0));
    while(t--){
        cin>>n;
        for(int i=0;i<n;i++){
            
        }
    }

    return 0;
}